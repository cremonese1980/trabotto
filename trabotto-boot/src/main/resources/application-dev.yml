spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trabotto
    username: trabotto
    password: trabotto
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
